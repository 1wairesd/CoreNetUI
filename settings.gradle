pluginManagement {
    plugins {
        id 'org.jetbrains.kotlin.jvm' version '2.1.20'
    }
}

rootProject.name = 'DiscordBM'

include(
        ':api',
        ':api:client-api',
        ':api:host-api',
        ':common',
        ':common:client-common',
        ':common:host-common',
        ':DiscordBMB',
        ':DiscordBMV'
)